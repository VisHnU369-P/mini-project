# Postgres (Neon) connection string
DATABASE_URL=psql  "postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}"

# Base URL for display in UI (no trailing slash)
BASE_URL=https://mini-project-production-d8d2.up.railway.app


# Server port (optional for local)
PORT=4000
